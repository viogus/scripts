name: Build & Push oci-helper Docker Image

on:
  push:
    branches:
      - main
    paths:
      - 'docker/oci-helper/**'
      - '.github/workflows/build-oci-helper.yml'
  schedule:
    - cron: '37 3 * * 0'
  workflow_dispatch:
    inputs:
      oci_helper_version:
        description: 'oci-helper version tag (e.g. v3.5.0)'
        required: false
        default: ''
        type: string

env:
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Determine version
        id: version
        run: |
          if [ -n "${{ inputs.oci_helper_version }}" ]; then
            echo "oci_helper_version=${{ inputs.oci_helper_version }}" >> $GITHUB_OUTPUT
          else
            VER=$(curl -s https://api.github.com/repos/Yohann0617/oci-helper/releases/latest \
              | grep '"tag_name":' | sed -E 's/.*"([^"]+)".*/\1/')
            if [ -z "$VER" ]; then
              echo "::warning::Failed to fetch latest oci-helper version, falling back to v3.5.0"
              VER="v3.5.0"
            fi
            echo "oci_helper_version=$VER" >> $GITHUB_OUTPUT
          fi

      - name: Check if image already published
        id: check
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ "${{ github.event_name }}" != "schedule" ]; then
            echo "skip=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          VER="${{ steps.version.outputs.oci_helper_version }}"
          EXISTS=$(gh api "orgs/${{ github.repository_owner }}/packages/container/oci-helper/versions" \
            --jq '.[].metadata.container.tags[]' 2>/dev/null | grep -Fx "main" || true)
          # just check if any build exists; rebuild on every cron to stay fresh
          echo "skip=false" >> $GITHUB_OUTPUT

      - name: Set up QEMU
        if: steps.check.outputs.skip != 'true'
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        if: steps.check.outputs.skip != 'true'
        uses: docker/setup-buildx-action@v3

      - name: Log in to ghcr.io
        if: steps.check.outputs.skip != 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        if: steps.check.outputs.skip != 'true'
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/oci-helper
          tags: |
            type=raw,value=latest
            type=raw,value=${{ steps.version.outputs.oci_helper_version }}

      - name: Build and push
        if: steps.check.outputs.skip != 'true'
        uses: docker/build-push-action@v6
        with:
          context: docker/oci-helper
          platforms: linux/amd64,linux/arm64
          build-args: |
            OCI_HELPER_VERSION=${{ steps.version.outputs.oci_helper_version }}
          labels: ${{ steps.meta.outputs.labels }}
          tags: ${{ steps.meta.outputs.tags }}
          push: true
          provenance: false
